---
- name: 修复/同步 Kodi 媒体路径内容类型
  hosts: localhost
  connection: local
  gather_facts: false
  vars_files:
    - ../group_vars/all.yml
  tasks:
    - name: 载入外部数据库配置
      ansible.builtin.import_tasks: load_db_secrets.yml

    - name: 确保安装了数据库依赖
      ansible.builtin.import_tasks: ensure_pymysql.yml

    - name: 确认 MyVideos131 数据库已存在
      delegate_to: localhost
      ansible.mariadb.mariadb_query:
        login_host: "{{ mariadb_host }}"
        login_port: "{{ mariadb_port }}"
        login_user: "{{ mariadb_user }}"
        login_password: "{{ mariadb_user_password }}"
        query: "SHOW DATABASES LIKE 'MyVideos131';"
      register: myvideos_check
      changed_when: false

    - name: 如果 MyVideos131 不存在则启动 Kodi 并等待建库
      when: myvideos_check.rowcount[0] == 0
      block:
        - name: 启动 Kodi 以触发数据库初始化
          ansible.builtin.command: "{{ adb_cmd }} shell am start -n org.xbmc.kodi/.Splash"
          delegate_to: localhost
          changed_when: true

        - name: 等待 MariaDB 中创建 MyVideos131 数据库
          delegate_to: localhost
          ansible.mariadb.mariadb_query:
            login_host: "{{ mariadb_host }}"
            login_port: "{{ mariadb_port }}"
            login_user: "{{ mariadb_user }}"
            login_password: "{{ mariadb_user_password }}"
            query: "SHOW DATABASES LIKE 'MyVideos131';"
          register: wait_db
          until: wait_db.rowcount[0] > 0
          retries: 12
          delay: 5
          changed_when: false

        - name: 自动关闭 Kodi 以释放数据库锁定
          ansible.builtin.command: "{{ adb_cmd }} shell am force-stop org.xbmc.kodi"
          delegate_to: localhost
          changed_when: true

    - name: 写入 Kodi 媒体路径与内容类型绑定关系
      delegate_to: localhost
      ansible.mariadb.mariadb_query:
        login_host: "{{ mariadb_host }}"
        login_port: "{{ mariadb_port }}"
        login_user: "{{ mariadb_user }}"
        login_password: "{{ mariadb_user_password }}"
        login_db: "MyVideos131"
        query: 
          - >
            INSERT IGNORE INTO path
            (strPath, strContent, strScraper, strSettings, scanRecursive, useFolderNames, noUpdate, exclude)
            VALUES
            ('{{ item.path }}', '{{ item.content }}', '{{ item.scraper }}', '', {{ item.scan_recursive }}, {{ item.use_folder_names }}, 0, 0);
          - >
            UPDATE path SET
            strContent = '{{ item.content }}',
            strScraper = '{{ item.scraper }}',
            scanRecursive = {{ item.scan_recursive }},
            useFolderNames = {{ item.use_folder_names }}
            WHERE strPath = '{{ item.path }}';
      loop: "{{ kodi_paths }}"
      loop_control:
        label: "{{ item.name }}"

    - name: 成功提示
      ansible.builtin.debug:
        msg: "✅ 所有路径的内容类型（movies/tvshows）均已强制同步至 MariaDB！"

    - name: 运行现有数据库排查报告
      ansible.builtin.import_tasks: check_db_tasks.yml
